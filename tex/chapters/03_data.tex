% =============================================================================
\chapter{Data}
\label{ch:data}
% =============================================================================

% TODO: Structure
%
% \section{Sample Selection}
% - Universe: top ~1,000 U.S. companies by market cap from SEC EDGAR
% - Filters: operating entities only, exclude SIC 6000--6999 (financials)
% - Final sample: ~600 non-financial firms, 2010--2025
% - Table~\ref{tab:sample}: industry distribution, FYE month distribution
%
% \section{Data Sources}
% \subsection{SEC EDGAR}
% - XBRL Company Facts API for financial statements (10-K)
% - Full-text filings for textual analysis (Item 7 MD\&A, Item 1A Risk Factors)
%
% \subsection{Stock Market Data}
% - Source: yfinance (adjusted close prices)
% - Volatility: annualised std of daily log returns over 365-day post-filing window
%
% \section{Financial Variables}
% - Raw XBRL metrics: total\_assets, net\_income, operating\_income,
%   operating\_cash\_flow, eps\_diluted, etc.
% - Derived ratios: log\_total\_assets, leverage, ROA, asset\_growth,
%   current\_ratio, accruals, ocf\_to\_assets, operating\_roa
% - Winsorisation at 1\%/99\%
%
% \section{Textual Variables}
% - $\Delta$Sentiment: YoY change in Loughran--McDonald net tone (Item 7)
% - $\Delta$Risk: YoY change in risk-word frequency (Item 1A)
% - TextSim: cosine similarity of consecutive filings
%
% \section{Divergence Measure Construction}
% - Financial trajectory: $\Delta$ROA = ROA$_t$ $-$ ROA$_{t-1}$
% - Narrative trajectory: $\Delta$Sentiment = Sent$_t$ $-$ Sent$_{t-1}$
% - Continuous: Divergence = $\Delta$Sentiment $-$ $\lambda \cdot \Delta$ROA
% - Binary: DivDummy = 1 if sign($\Delta$Sentiment) $\neq$ sign($\Delta$ROA)
%
% \section{Dependent Variable}
% - Primary: annualised volatility (std of daily log returns $\times \sqrt{252}$)
%   over 365-day post-filing window
% - Robustness: post-filing log stock return
%
% \section{Panel Data Construction}
% - Annual panel: ~9,000 firm-years
% - Unbalanced panel, fiscal year-end alignment
%
% \section{Descriptive Statistics}
% - Summary statistics (Table~\ref{tab:desc_stats})
% - Correlation matrix (Table~\ref{tab:correlations})


% ─────────────────────────────────────────────────────────────────────────────
\section{Filing Frequency}
\label{sec:annual_vs_quarterly}
% ─────────────────────────────────────────────────────────────────────────────

Before proceeding to variable construction, a brief note on why this study
works with annual 10-K filings rather than quarterly 10-Q reports is in order.
Although quarterly data would roughly triple the sample, three practical
obstacles---documented in Tables~\ref{tab:xbrl_coverage}--\ref{tab:text_availability}---ruled
out that path.

\paragraph{Financial data gaps in Q4.}
10-Q filings cover only Q1 through Q3; the fourth quarter must therefore be
backed out as the residual between the annual figure and the three preceding
quarters. Table~\ref{tab:q4_reliability} shows that complete Q1--Q3 XBRL
data exist for roughly 80\% of firm-years in net income and operating income,
but for only 7\% in operating cash flow---because most firms report
cumulative year-to-date cash flows rather than standalone quarterly amounts.
Where both an explicit Q4 fact and the implied residual can be compared,
about 11\% of observations diverge by more than 1\% of the annual total.
Because accruals depend directly on operating cash flow, the quarterly route
would have made hypothesis~H3 untestable.

\input{tables/q4_reliability}

\paragraph{Missing risk-factor text in 10-Q.}
SEC Regulation S-K requires firms to update risk factors in quarterly filings
only when ``material changes'' have occurred since the last 10-K.  Practically
speaking, just over half the 10-Q filings we sampled (15 firms, 90 filings)
contained a substantive risk-factor section; the rest reduced to a one-sentence
reference back to the annual report (Table~\ref{tab:text_availability}).
This pattern held across sectors---technology, healthcare, consumer goods,
energy---with only occasional exceptions.  Constructing $\Delta$Risk, which
enters both H1 and H2, is therefore not feasible at a quarterly frequency.

\input{tables/text_availability}

\paragraph{Chosen frequency.}
On these grounds---and in line with prior textual-analysis work based on
10-K filings \parencite{loughran2011,li2008,blankespoor2020}---we adopt an
annual panel. The XBRL coverage comparison (Table~\ref{tab:xbrl_coverage})
and a consolidated summary (Table~\ref{tab:annual_vs_quarterly}) are
included in Appendix~\ref{app:diagnostics} for completeness.

% Full diagnostic tables deferred to appendix:
% \input{tables/xbrl_coverage}       → Appendix
% \input{tables/annual_vs_quarterly} → Appendix


% ─────────────────────────────────────────────────────────────────────────────
\section{XBRL Tag Consistency}
\label{sec:xbrl_tags}
% ─────────────────────────────────────────────────────────────────────────────

A practical challenge of the XBRL Company Facts API is that a single
accounting concept may be reported under alternative US-GAAP taxonomy tags.
For instance, operating cash flow appears as either
\texttt{NetCashProvided\-ByUsed\-InOperating\-Activities} (total, including
discontinued operations) or
\texttt{...ContinuingOperations} (continuing operations only).
Net income may be tagged as \texttt{NetIncomeLoss}, \texttt{ProfitLoss},
or \texttt{IncomeLossFrom\-ContinuingOperations}.
Stockholders' equity can include or exclude the non-controlling interest.
Although the concepts overlap substantially, they are not identical:
combining them within a single firm's time series could introduce
artificial breaks, while selecting one tag globally would discard firms
that consistently report under the alternative.

We resolve this with a \emph{per-firm tag-locking} procedure.
For each (firm, metric) pair where multiple source tags exist, we count
which XBRL tag each firm reports in the most fiscal years, lock the firm
to that dominant tag, and set values from non-dominant tags to missing.
This ensures within-firm time-series consistency---the critical requirement
for panel regressions that identify effects from within-firm variation.

The approach is justified on three grounds.
First, cross-sectional comparability is a secondary concern in our
specification because industry and year fixed effects absorb level
differences between tag definitions, and standard errors are clustered
by firm \parencite{sloan1996}.
Second, the practical discrepancy between alternative tags is negligible
for the vast majority of observations: for firms that report \emph{both}
total and continuing-operations OCF in the same filing, the median
difference is below 0.5\% of the larger value, with over 90\% of
overlapping observations showing an exact match.  The gap arises only in
the specific year a firm acquires or divests a segment---a single-year
event that winsorisation at the 1st and 99th percentiles clips.
Third, the US~GAAP XBRL taxonomy explicitly nests the alternative tags
under the same balance-sheet or income-statement line item;
\textcite{chen2019} and the SEC's own interactive-data rules
\parencite{sec2009} treat them as equivalent for consistency-check
purposes.

One residual limitation deserves mention.  The accruals ratio is
defined as $(\text{NI} - \text{OCF}) / \text{TA}$, so its
interpretation hinges on NI and OCF measuring income and cash flow at
the \emph{same} scope---total or continuing operations.  Our tag-locking
procedure enforces consistency \emph{within} each metric, but not
\emph{across} metrics: 22~firms (approximately 4\% of the sample) lock
to the continuing-operations OCF tag while using total net income.  For
these firms the accruals ratio inadvertently captures the cash-flow
effect of discontinued operations, introducing modest noise.  We
consider this acceptable for three reasons: the affected share is small,
winsorisation at the 1st and 99th percentiles clips the most extreme
values, and the same cross-tag mismatch is present---and typically
unaddressed---in the large-scale XBRL studies we build on
\parencite{chen2019}.

Per-firm tag choices and the overlap diagnostic are reported in
Appendix~\ref{app:variables}
(Table~\ref{tab:tag_provenance}).
