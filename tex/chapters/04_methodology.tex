% =============================================================================
\chapter{Methodology}
\label{ch:methodology}
% =============================================================================

% TODO: Structure
%
% \section{Empirical Framework}
% - Panel regression setting
% - Primary DV: annualised post-filing volatility
% - Three nested models: baseline → text → divergence
%
% \section{Baseline Model (Financial + Lagged Volatility)}
% \begin{equation}
%   \sigma_{i,t+1} = \alpha + \phi\,\sigma_{i,t}
%              + \beta_1 \ln(\text{Assets}_{i,t})
%              + \beta_2 \text{Leverage}_{i,t}
%              + \beta_3 \text{ROA}_{i,t}
%              + \beta_4 \text{AssetGrowth}_{i,t}
%              + \varepsilon_{i,t}
% \end{equation}
% Note: lagged vol (\sigma_{i,t}) absorbs volatility persistence.
%
% \section{Text Model (Financial + Textual Features)}
% \begin{equation}
%   \sigma_{i,t+1} = \alpha + \phi\,\sigma_{i,t}
%              + \boldsymbol{\beta}' \mathbf{X}^{\text{fin}}_{i,t}
%              + \gamma_1 \Delta\text{Sent}_{i,t}
%              + \gamma_2 \Delta\text{Risk}_{i,t}
%              + \gamma_3 \text{TextSim}_{i,t}
%              + \varepsilon_{i,t}
% \end{equation}
% Tests H1: textual change predicts vol beyond financials and past vol.
%
% \section{Divergence Model}
% \begin{equation}
%   \sigma_{i,t+1} = \alpha + \phi\,\sigma_{i,t}
%              + \boldsymbol{\beta}' \mathbf{X}^{\text{fin}}_{i,t}
%              + \boldsymbol{\gamma}' \mathbf{X}^{\text{text}}_{i,t}
%              + \delta \cdot \text{Div}_{i,t}
%              + \varepsilon_{i,t}
% \end{equation}
% Tests H2: divergence predicts vol beyond both text and financials.
%
% \section{Estimation}
% - OLS with industry (SIC-2) and year fixed effects
% - Standard errors clustered by firm
%
% NOTE (from pipeline audit): 60\% of consecutive (firm, year) return
% windows overlap because the 365-day post-filing window often extends
% past the next year's filing date.  This creates serial correlation in
% both return\_next\_year and vol\_next\_year within firms.  Firm-clustered
% standard errors (Petersen 2009) are robust to arbitrary within-firm
% dependence, so no adjustment is needed---but the overlap should be
% acknowledged here when writing this section.
%
% NOTE (from pipeline audit): 12~firms changed their fiscal year-end
% during the sample period (e.g.~ADM from June to December, BBY from
% March to January).  The transition year is removed by the pipeline,
% leaving one multi-year gap in the firm's time series.  Asset growth is
% set to NaN at the gap; lagged volatility and returns are unaffected
% because they are anchored to filing dates.  Mention this in the
% estimation or data-construction discussion.
%
% \section{Model Comparison}
% - Incremental $R^2$ across the three nested models
% - F-test for joint significance of textual / divergence terms
% - Vuong (1989) test for non-nested model comparison
